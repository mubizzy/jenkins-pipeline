apiVersion: apps/v1
kind: Deployment
metadata:
  name: mydeploy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mydeploy1
  template:
    metadata:
      app: mypod1
      labels:
        app: mydeploy1
    spec:
      containers:
      - name: mycontainer
        image: mubii2/ci-pipeline:latest   # replace with your Docker-image pushed on Dockerhub
        ports:
        - containerPort: 8080
